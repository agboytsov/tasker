summary: Возвращает одну конкретную задачу
parameters:
  - name: task_id
    in: path
    type: integer
    required: true
    description: ID статьи
definitions:
  Task:
    type: object
    properties:
      title:
        type: string
      id:
        type: integer
      description:
        type: string
      created_at:
        type: string
        format: date-time
      updated_at:
        type: string
        format: date-time
responses:
  200:
    schema:
      $ref: '#/definitions/Task'
  404:
    schema:
      type: object
      properties:
        error:
          type: string

